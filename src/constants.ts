// Fetch Util
const INVALID_CRED_KEY = 'invalidCredentials';
const SERVER_ERROR_KEY = 'internalServerError';
const INSUFFICIENT_PRIVILEGES = 'insufficientPrivileges';

// Application util
const SCALAR_E7 = 0.0000001;

// Broadcast channel
const LOGIN_EVENTS_CHANNEL = 'login-events';

// Broad messages
const LOGIN_REQUIRED = 'login_required';

export {
  LOGIN_REQUIRED,
  INVALID_CRED_KEY,
  SERVER_ERROR_KEY,
  INSUFFICIENT_PRIVILEGES,
  SCALAR_E7,
  LOGIN_EVENTS_CHANNEL
};
